<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="row">
      <div class="large-12 columns">

        <nav class="top-bar" data-topbar>
          <ul class="title-area">
            <li class="name">
              <h1><%= link_to 'MyFlashcards', '/' %></h1>
            </li>
            <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
          </ul>

          <section class="top-bar-section">
            <!-- Right Nav Section -->
            <ul class="right">
              <% if current_user %>
                <li><%= link_to 'Review', review_index_path %></li>
                <li><%= link_to 'Manage Decks', decks_path %></li>
                <li><%= link_to 'Manage Flashcards', flashcards_path %></li>
                <li class="has-form">
                  <%= link_to "Logout", logout_path, class: "button" %>
                </li>
              <% else %>
                <li class="has-form">
                  <%= link_to "Register", signup_path %>
                </li>
                <li class="has-form">
                  <%= link_to "Login", login_path, class: "button" %>
                </li>
              <% end %>
            </ul>
          </section>
        </nav>
      </div>
    </div>

    <div class="row">
      <div class="large-12 columns">

        <div class="row" id="ajax-delete-flashcard">
          <div class="large-12 columns">
            <div data-alert class="alert-box">
              Flashcard has been deleted.
            </div>
          </div>
        </div>

        <div class="row" id="ajax-delete-deck">
          <div class="large-12 columns">
            <div data-alert class="alert-box">
              Deck has been deleted.
            </div>
          </div>
        </div>

        <% flash.each do |name, message| %>
        <div class="row" id="homepage-alert">
          <div class="large-12 columns">
            <div data-alert class="alert-box">
              <%= message %>
            </div>
          </div>
        </div>
        <% end %>

        <%= yield %>
        
      </div>
    </div>

    <%= javascript_include_tag "application" %>
  </body>
</html>